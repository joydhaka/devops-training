FROM alpine

RUN apk add -v --update \
            python \
            py-pip \
            groff \
            less \
            mailcap && \
        pip install --upgrade awscli s3cmd python-magic && \
        apk -v --purge del py-pip && \
        rm /var/cache/apk/*
